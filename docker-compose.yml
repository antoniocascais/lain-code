services:
  lain:
    build: .
    container_name: lain-code
    user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "8000:8000"
    environment:
      - LAIN_DATA_DIR=/data
    volumes:
      - ${CLAUDE_PROJECTS:-~/.claude/projects}:/data:ro
